% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet.R
\name{join_model_tables}
\alias{join_model_tables}
\title{Join the three v4 tables to one row per specification with full context}
\usage{
join_model_tables(tables)
}
\arguments{
\item{tables}{A list as returned by \code{read_dist_tables()}}
}
\value{
A tibble with one row per model spec.
}
\description{
Both \code{models} and \code{model_specs} carry \code{taxa}/\code{region}/\code{component}/
\code{descriptors} columns; the spec-level scope is the fallback-resolved scope
that applies to the individual model, so it is retained as \verb{spec_*} and the
model-level scope is dropped. Publication columns are carried through as-is.
}
\keyword{internal}
