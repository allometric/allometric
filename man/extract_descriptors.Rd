% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_tbl.R
\name{extract_descriptors}
\alias{extract_descriptors}
\title{Extract descriptors of a \code{model_tbl} into columns}
\usage{
extract_descriptors(data, ...)
}
\arguments{
\item{data}{A \code{model_tbl}}

\item{...}{Descriptor names to extract, given with tidyselect semantics
(e.g. \code{country}, or \code{dplyr::any_of(c("country", "p"))}). When a name is
not found, or nothing is selected, an error lists the descriptors
available in the table.}
}
\value{
A \code{model_tbl} with the requested descriptor columns added after
the existing columns. Each model contributes one row, so the number of
rows is unchanged.
}
\description{
Descriptors describe the context of an allometric model, such as the
country or region where the model data were collected, the processing
group or equation of a model set, or any other fields declared by the
publication. A \code{model_tbl} surfaces a few curated descriptors as columns
(\code{taxa}, \code{region}, \code{component}); the rest are stored on each model object.
This function widens the requested descriptors into columns of the table,
one row per model, so they can be filtered, selected, and browsed like any
other column.
}
\details{
Scalar descriptors (e.g. \code{country}, \code{proc_group}, \code{p}) become atomic
columns, with \code{NA} where a model does not declare the descriptor.
Descriptors that are multi-valued in any row (e.g. a model applicable to
several regions) are returned as list columns, matching the style of the
\code{region} column; rows that do not declare the descriptor hold \code{NULL}.
Descriptor columns that are already present in the table are returned
unchanged.
}
\examples{
models <- load_models()

models |>
  extract_descriptors(country, proc_group) |>
  dplyr::filter(proc_group == "taxa")
}
