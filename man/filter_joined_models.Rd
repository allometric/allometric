% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load.R
\name{filter_joined_models}
\alias{filter_joined_models}
\title{Filter a joined v4 table before model reconstruction}
\usage{
filter_joined_models(joined, model_type = NULL, country = NULL, region = NULL)
}
\arguments{
\item{joined}{The tibble returned by \code{join_model_tables()}}

\item{model_type}{An optional character vector of model types (e.g.
\code{"stem volume"}). Only models whose response matches one of the given
types are kept.}

\item{country}{An optional character vector of ISO country codes (e.g.
\code{"CA"}). Region codes embed the country as a prefix (e.g. \code{"US-CO"}).}

\item{region}{An optional character vector of region codes (e.g. \code{"US-CO"}).}
}
\value{
The filtered joined tibble
}
\description{
Filters are applied on the joined table (before \code{build_model_tbl}), so only
the requested models are ever constructed. \code{model_type} is derived from the
response name via \code{get_model_type()}; \code{country} and \code{region} are matched
against the spec-level \code{spec_region} list column (region codes embed the
country as a dash-prefixed code, e.g. \code{"US-CO"}).
}
