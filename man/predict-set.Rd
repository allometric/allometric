% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict-set.R
\name{predict,list-method}
\alias{predict,list-method}
\alias{predict,model_tbl-method}
\title{Predict with a set of allometric models}
\usage{
\S4method{predict}{list}(model, ..., output_units = NULL)

\S4method{predict}{model_tbl}(model, ...)
}
\arguments{
\item{model}{A list of allometric models, or a \code{model_tbl} with one model
per row}

\item{...}{Covariate values passed to each model's \code{predict_fn}. Named
arguments are matched to models by covariate name; unnamed arguments are
matched positionally}

\item{output_units}{Optionally specify the output units of the predictions
as a string, e.g., \code{"ft^3"}}
}
\value{
A vector of predictions. When models have units, the result carries
the response units of the first model (converting the others as needed);
pass \code{output_units} to convert explicitly.
}
\description{
\code{predict} accepts a list of models (for example, a list-column in a data
frame) and applies each model to the covariate values for its row. This
makes it straightforward to predict with many models in a single
\code{dplyr::mutate()} call:
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{data |>
  dplyr::left_join(my_models, by = "SPCD") |>
  dplyr::mutate(vol = predict(model, dsob = DIA * 2.54, hst = HT * 0.3048))
}\if{html}{\out{</div>}}

Covariate arguments may be unnamed, in which case they are matched
positionally to each model's covariates and all models must share the same
functional form. Alternatively, arguments may be named by covariate, in
which case each model is matched by name and only the covariates it
requires are used; models whose covariates are not all supplied produce
\code{NA} with a warning. This allows sets of models with different functional
forms and covariate orders to be predicted in a single call.

Each covariate argument must be length 1 or the number of models, and is
recycled as necessary. Rows where the model is \code{NA} (e.g., unmatched rows
after a \code{dplyr::left_join()}) produce \code{NA}.
}
